import db from '../../database/index';

const getUser = function(session_id: string) {
  // using the session id grab the username
  console.log('getUserProfile activated');
}

export default getUser;
