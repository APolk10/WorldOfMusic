import db from '../../database/index';

const removeFavorite = function(user: string, artist: string) {
  // find the favorite that matches the user in the favorites table and delete it
  console.log('removeFavorite activated');
}

export default removeFavorite;