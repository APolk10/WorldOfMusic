import db from '../../database/index';

const trackClick = function(country: string) {
  // update the global counter for this country
  console.log('trackClick activated');
}

export default trackClick;
